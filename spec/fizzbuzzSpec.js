describe("My Fizzbuzz", function() {
    beforeEach(function(){ 
        number = new fizzbuzz;
    });
    

    describe("test is number is divisible by 3 and 5, then return 'FizzBuzz'", function() {
        it("should return 'FizzBuzz'", function() {
            var expect=15;
            expect(calc.value).toBe();
        });
        it("should return 'Fizz'", function() {
            var expect=15;
            expect(calc.value).toBe();
        });
        it("should return 'Buzz'", function() {
            var expect=15;
            expect(calc.value).toBe();
        });